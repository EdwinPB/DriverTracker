import Core
import Domain
import Networking
import Storage

/// Offline-first sync: change queue, push/pull, conflict resolution.
/// Namespace only — feature code lands here later.
public enum SyncEngine {}
